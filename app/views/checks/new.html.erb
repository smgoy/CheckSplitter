<div class="col-sm-8 col-sm-offset-2">
  <h3>I just need some basic information about your check.</h3>
</div>

<div class="well well-lg col-sm-8 col-sm-offset-2">
  <%= form_for @check do |form| %>

    <div class="form-group">
      <% #form.label :tax %>
      <%= form.text_field :tax, class: "form-control", placeholder: "Total Tax" %>
    </div>

    <div class="form-group">
      <% #form.label :total %>
      <%= form.text_field :total, class: "form-control", placeholder: "Total Cost of Your Bill" %>
    </div>

    <div class="form-group">
      <% #form.label :tip %>
      <%= form.text_field :tip, class: "form-control", placeholder: "Tip Percentage (ex: 20%)" %>
    </div>

    <%= form.submit "Continue", class: "btn btn-primary btn-sm" %>

  <% end %>
</div>
